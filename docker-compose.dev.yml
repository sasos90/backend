version: '3'
services:
  api:
    build:
      context: .
      dockerfile: ./apps/api/dev.dockerfile
    environment:
      port: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./apps/api:/app/apps/api
